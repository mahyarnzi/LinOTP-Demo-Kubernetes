This project was implemented by IT R&D team.
Used:
    1. Image "{{ .Values.linotp.mysql.containers.main.image }}" for LinOTP database.
    2. Image "{{ .Values.radius.image }}" for Radius server
    3. Image "{{ .Values.linotp.image }}" for LinOTP application.

Contact Us:
    Mahyar Nazari
    mahyar.nzi@gmail.com
    https://github.com/mahyarnzi/linotp
